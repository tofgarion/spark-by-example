with Ada.Text_IO; use Ada.Text_IO;
with Chap3;
with Chap4;
with Chap5;
with Chap6;
procedure Tests with
   Spark_Mode => On is

begin
--   Put_Line (Boolean'Image (Equal (A, B, A'Length)));
   null;
end Tests;
